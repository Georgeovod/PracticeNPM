'use strict'

const sum = (num1, num2) => {
    let result = num1 + num2;
    return result;
};

const subtraction = (num1, num2) => {
    let result = num1 - num2;
    return result;
};

const multiplay = (num1, num2) => {
    let result = num1 * num2;
    return result;
};